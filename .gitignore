# BMAD-Method
_bmad/
_bmad-output/

# Antigravity Agent
.agent/

# Gemini CLI
.gemini/

# Dependencies
node_modules

# Build artifacts
output.css
# NOTE: Tailwind.html is a build artifact but MUST be tracked in git
# because clasp push reads .gitignore and would skip it,
# breaking the GAS web app (Index.html includes it via HtmlService).

# OS-specific
.DS_Store

# Deployment configuration (contains project-specific IDs)
.clasp.json

# Code analysis output (local-only, post-commit hook auto-rebuilds)
graphify-out/
.graphify*

# Environment / config files
*.env
.phpunit.result.cache
